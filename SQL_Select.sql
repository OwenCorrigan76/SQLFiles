use library;

show tables;

select * from book;

desc book;

select title, category
from book;

select * from book;

select title, category, price
from book
where price >=50;



select * from student;
